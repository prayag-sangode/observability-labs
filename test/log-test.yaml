apiVersion: v1
kind: Pod
metadata:
  name: log-test
  labels:
    app: log-test
spec:
  containers:
  - name: logger
    image: busybox
    args:
      - /bin/sh
      - -c
      - |
        while true; do
          echo "$(date) - log from busybox";
          sleep 5;
        done
